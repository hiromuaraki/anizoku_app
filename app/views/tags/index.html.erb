<div class="row" style="margin-top: 50px">
  <div class="col-md-8">
  </div>
  <div class="col-md-4">
  </div>
</div>
<div class="mx-auto" style="width: 930px">
<h3><i class="fas fa-search"></i>タグからアニメを探す</h3>
  <div class="row">
    <% @tags.each_with_index do |tag, i| %>
      <div class="col-md-3">
        <span class="btn btn-secondary list-group">
          <%= tag.name %>（<%= @tag_group_count[i][1].size%>件）
        </span>
      </div>
    <% end %>
  </div>
</div>
